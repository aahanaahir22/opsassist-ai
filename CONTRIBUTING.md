# Contributing

1. Create a focused branch from `main`.
2. Add or update tests for behavior changes.
3. Run `pytest -q --cov=app` in `backend/`.
4. Run `npm run build` in `frontend/`.
5. Explain safety implications in the pull request.

Do not add real credentials, production endpoints, customer logs, or unrestricted shell execution.

