# Five-minute recruiter demo

1. Open the command center and point out the **SIMULATED PROTOTYPE DATA** disclosure.
2. Show three database timeout events grouped into one critical payment incident.
3. Open Diagnosis and trace the root cause to the stable `RB-PAY-001` evidence IDs.
4. Explain the 0.88 confidence and the typed rolling-recycle action plan.
5. Click **Approve plan** and show the named approver entering the timeline.
6. Click **Execute safely** and show the verified healthy simulator state.
7. Open `/docs` to demonstrate the REST surface and schemas.
8. Run `pytest -q --cov=app` to show the safety path is integration-tested.
9. Close by stating the boundary: no production system is connected; this is evidence of backend, AI retrieval, safety, API, database, frontend, testing, and Docker skills.

